version: "3.9"
services:
  api:
    build: .
    image: everestctl-api:local
    environment:
      - ADMIN_API_KEY=${ADMIN_API_KEY:-changeme}
      - PORT=8080
      - KUBECONFIG=/root/.kube/config
    ports:
      - "8080:8080"
    volumes:
      - ${HOME}/.kube/config:/root/.kube/config:ro

